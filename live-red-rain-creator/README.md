# 红包雨批量创建工具

读取业务 Excel，复用直播运营工具的京东登录态，在京东直播后台批量创建 CMC 红包雨活动。

业务用户统一从 `live-web` 页面进入，不需要单独启动本模块。

## 输入字段

- 活动名称
- 开始时间
- 结束时间
- 红包发放方式
- 红包ID
- 中奖概率（普通发放必填，人群策略留空）

详细业务规则参见 `../docs/sop/直播-自动创建红包雨-SOP/直播-自动创建红包雨-SOP.md`。
